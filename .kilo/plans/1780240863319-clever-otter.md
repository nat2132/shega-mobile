# Plan: Recent Activity, Font Size, Graph, and Category Fixes

## Overview
Fix multiple UI/UX issues across dashboard, inventory, sales, and adjustment screens.

## Changes Required

### 1. Recent Activity Limit (5 items)
**Files to modify:**
- `src/screens/dashboard/dashboard.tsx` - Change `getActivityFeed(4)` to `getActivityFeed(5)` on line 193
- `src/screens/inventory/inventroy.tsx` - Change `getRecentItems(10)` to `getRecentItems(5)` on line 187
- `src/screens/sales/sales.tsx` - Change `getRecentSales(10)` to `getRecentSales(5)` on line 91
- `src/screens/adjustement/adjustment.tsx` - Change `getRecentAdjustments()` (default 20) to `getRecentAdjustments(undefined, 5)` on line 143
- `src/screens/expense/expense.tsx` - Already uses 5, no change needed

### 2. Adjustable Font Size for Big Numbers
**Files to modify:**
- `src/screens/dashboard/dashboard.tsx`:
  - Add dynamic font size calculation for `metricValue` based on number length
  - Use inline style adjustment for `metrics.today.revenue` and other large numbers
- `src/screens/sales/sales.tsx`:
  - Add dynamic font size for `totalRevenueVal` (line 686) and `bentoValue` (line 778)
- `src/screens/inventory/inventroy.tsx`:
  - Add dynamic font size for `vaultValue` (line 909) and `miniValue` (line 944)

### 3. Sales - Show Full Numbers (No k/M Abbreviation)
**Files to modify:**
- `src/screens/sales/sales.tsx`:
  - Replace `formatAbbreviated(totalRevenue)` with `totalRevenue.toLocaleString()` on line 231
  - Replace `formatAbbreviated(val)` with full number in chart tooltip on line 160
  - Ensure `formatAbbreviated` is not used for main sales numbers

### 4. Dashboard Graph Adjustment Based on Today vs Yesterday
**Files to modify:**
- `src/screens/dashboard/dashboard.tsx`:
  - Modify `SparklineChart` component to accept today/yesterday values
  - Adjust chart curve path based on the comparison:
    - If today > yesterday: ascending curve (positive trend)
    - If today < yesterday: descending curve (negative trend)
    - If equal or no data: neutral curve
  - Update the percentage calculation to reflect actual comparison

### 5. Remove Default Categories from Inventory
**Files to modify:**
- `src/screens/inventory/inventroy-form.tsx`:
  - Remove the `DEFAULT_CATEGORIES` constant (lines 72-84)
  - Change categories state initialization to empty array
  - Remove `seedDefaultCategories` call from `loadCategories` effect
- `src/database/db.ts`:
  - `seedDefaultCategories` function can remain but won't be called

## Implementation Order
1. Fix recent activity limits (simple, isolated changes)
2. Remove default categories (affects database seeding)
3. Add dynamic font sizing (requires style computation)
4. Update sales to show full numbers (replace format calls)
5. Update dashboard graph comparison logic